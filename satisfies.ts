const paletee = {
    red: [255, 0, 0],
    green: "#0f0000",
    blue: [0, 255, 0]
}
const redComponent = paletee.red.toLocaleString();

const greenComponent = paletee.green.toUpperCase();
// console.log(greenComponent);
console.log(redComponent);